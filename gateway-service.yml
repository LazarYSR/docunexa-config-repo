spring:
  application:
    name: gateway-service
server:
  port: 8080

spring:
  cloud:
    gateway:
      routes:
        - id: test-route
          uri: https://httpbin.org
          predicates:
            - Path=/test/**